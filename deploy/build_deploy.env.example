
# Local project paths
ROOT_DIR="$(pwd)/.."
ANGULAR_DIR="$ROOT_DIR/frontend"
ASP_DIR="$ROOT_DIR/backend"
RELEASE_DIR="$ROOT_DIR/deploy/release"
ASP_DOCKERFILE="$ASP_DIR/Dockerfile.prod"

# Remote server (VPS) settings
VPS_SSH_KEY_PATH="PATH_TO_YOUR_SSH_KEY"
VPS_SSH_PORT='THE_SSH_PORT_YOUR_SERVER_USES'
VPS_USER=""
VPS_HOST=""
VPS_SSH="$VPS_USER@$VPS_HOST"

VPS_WORKTREE="/var/www/taletoss"

# Docker
DOCKER_COMPOSE_FILE="docker-compose.prod.yml"
